# Producer_Consumer_Location

## Installation
------------

Open command line and run the following commands to setup

1. `git clone https://github.com/AnjulaAbey/Producer_Consumer_Location.git`
2. `cd Producer_Consumer_Location`
